[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Los tests cubren los MOTORES DETERMINISTAS del Job Agent (firewall, scoring,
# personalización, insights): funciones puras, sin BD ni red ni IA → rápidos y
# estables. Correr desde services/api: `venv/bin/python3.12 -m pytest`.
