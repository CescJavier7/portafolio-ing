# Dependencias SOLO para desarrollo/tests. NO se instalan en la imagen de
# producción (el Dockerfile usa requirements.txt). Instalar en el venv con:
#   venv/bin/pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
